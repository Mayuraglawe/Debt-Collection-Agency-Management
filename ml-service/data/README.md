# Data Directory

This directory contains datasets and processed data for ML models.

## Structure

- `raw/` - Original, immutable data
- `processed/` - Cleaned and preprocessed data
- `features/` - Feature engineered datasets

## Note

Large data files are gitignored. Store them locally or use a data versioning tool like DVC.
